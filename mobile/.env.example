# Copy to .env and fill in values
# Production (default when no .env present)
EXPO_PUBLIC_API_URL=https://gymcoach.vercel.app

# Local dev: replace with your Mac's LAN IP while running `node server/index.js`
# EXPO_PUBLIC_API_URL=http://192.168.1.X:3000
